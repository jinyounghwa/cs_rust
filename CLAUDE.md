# cs-bite — CLI 대기 시간 CS 공부 도구

## 목표
Claude Code / AI CLI 실행 후 대기하는 2~5분 동안
Rust 코드 예제 기반 CS 개념을 터미널에 출력한다.
별도 추적 없이, 실행할 때마다 새 토픽이 뜨면 된다.

## 사용자
진영화 본인 전용 — 1인 로컬 도구.
NestJS 실무 경험 있음. Rust는 학습 중.
타겟: 백엔드 & 로컬 AI 인프라 설계력.

## 핵심 기능
1. `cs-bite` 실행 → 랜덤(or 순서) Rust 예제 1개 출력
2. 예제 = 개념 설명 + 실행 가능한 Rust 코드 + 핵심 포인트 3줄
3. 대기 시간(2~5분) 안에 읽고 이해할 수 있는 분량
4. 선택: `cs-bite run` → 예제 코드 직접 `cargo run` 실행

## 스코프
- Rust 단독 바이너리 (cargo 프로젝트)
- 토픽 콘텐츠: 소스 파일(.rs) 또는 내장 상수로 관리
- 외부 DB, 서버, 네트워크 없음
- 진행 추적 없음 — 단순 출력
- 컬러 터미널 출력 (colored 또는 termcolor crate)
